<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="query_author_list">
{
  authors(first:10000) {
    edges {
      node {
        id
        firstName
        lastName
        hasCollidingName
        totalReports
        extra
      }
    }
  }
}
    </string>
    <string name="query_report_list">
query ($first: Int!, $after: String, $query: String){
  searchReports(first:$first, after:$after, query:$query) {
    pageInfo {
      endCursor
    }
    edges {
      node {
        id
        date
        author {
          id
          firstName
          lastName
          extra
        }
        body
      }
    }
    totalCount
  }
}
    </string>
</resources>